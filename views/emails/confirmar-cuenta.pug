<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        style
            include ./email.css
        
    body
        h2 Confirma tu cuenta Uptask
        p ya esta casi inscrito, solo confirma tu cuenta
        a(href=`${confirmarUrl}`) Confirmar cuenta
        p Si no puedes acceder a este enlace, visita #{confirmarUrl}
        p Si no solicitaste este e-mail, puedes ignorarlo